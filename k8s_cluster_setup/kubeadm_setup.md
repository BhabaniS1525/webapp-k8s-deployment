# setting k8s production cluster using kubeadm

This guide walks you through setting up a production-grade Kubernetes cluster using `kubeadm`. This method is suitable for multi-node clusters and provides more control over the configuration compared to single-node solutions like Minikube.
